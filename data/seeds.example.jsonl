{"id":"synthetic-bias-gelu-001","operation":"fused bias plus GELU","objective":"Replace a bias addition followed by exact GELU with one fused device kernel while preserving float32 behavior and arbitrary contiguous M by N inputs.","backend":"triton","constraints":["Support N values that are not powers of two","Use explicit tail masks","Do not call torch.nn.functional.gelu in ModelNew"],"reference_code":"import torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\nclass Model(nn.Module):\n    def __init__(self, width: int):\n        super().__init__()\n        self.bias = nn.Parameter(torch.randn(width))\n\n    def forward(self, x):\n        return F.gelu(x + self.bias, approximate=\"none\")\n\ndef get_inputs():\n    return [torch.randn(37, 513, device=\"cuda\", dtype=torch.float32)]\n\ndef get_init_inputs():\n    return [513]\n","metadata":{"family":"elementwise-fusion","license":"CC0-1.0","origin":"original-synthetic-seed"}}
{"id":"synthetic-rmsnorm-001","operation":"row-wise RMS normalization","objective":"Implement row-wise RMS normalization with a learned scale and stable float32 accumulation for a non-power-of-two hidden size.","backend":"triton","constraints":["Input dtype is float16 but reduction accumulation must be float32","Return float16 output","Handle hidden size 768 without out-of-bounds reads","Do not invoke a framework normalization operator in ModelNew"],"reference_code":"import torch\nimport torch.nn as nn\n\nclass Model(nn.Module):\n    def __init__(self, width: int, eps: float = 1e-6):\n        super().__init__()\n        self.weight = nn.Parameter(torch.ones(width, dtype=torch.float16))\n        self.eps = eps\n\n    def forward(self, x):\n        variance = x.float().pow(2).mean(dim=-1, keepdim=True)\n        normalized = x.float() * torch.rsqrt(variance + self.eps)\n        return (normalized * self.weight.float()).to(x.dtype)\n\ndef get_inputs():\n    return [torch.randn(29, 768, device=\"cuda\", dtype=torch.float16)]\n\ndef get_init_inputs():\n    return [768, 1e-6]\n","metadata":{"family":"reduction-normalization","license":"CC0-1.0","origin":"original-synthetic-seed"}}
{"id":"synthetic-gated-silu-001","operation":"gated SiLU elementwise transform","objective":"Fuse x times SiLU(gate) into a CUDA extension kernel for float32 tensors with arbitrary element counts.","backend":"cuda","constraints":["Use torch.utils.cpp_extension.load_inline for the wrapper","Bounds-check the final block","Do not use torch.sigmoid, torch.silu, or torch.nn.functional.silu in ModelNew"],"reference_code":"import torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\nclass Model(nn.Module):\n    def forward(self, x, gate):\n        return x * F.silu(gate)\n\ndef get_inputs():\n    return [\n        torch.randn(17, 129, device=\"cuda\", dtype=torch.float32),\n        torch.randn(17, 129, device=\"cuda\", dtype=torch.float32),\n    ]\n\ndef get_init_inputs():\n    return []\n","metadata":{"family":"elementwise-fusion","license":"CC0-1.0","origin":"original-synthetic-seed"}}
